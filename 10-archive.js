const d=(()=>{try{return JSON.parse(localStorage.getItem('yashmi-content'))||YASHMI_CONTENT}catch(e){return YASHMI_CONTENT}})(),$=s=>document.querySelector(s),esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let section=new URLSearchParams(location.search).get('section');if(!['play','notes','work'].includes(section))section='play';document.title=`${section[0].toUpperCase()+section.slice(1)} - Yashmi Adani`;$('#archive-title').textContent=section;const dialog=$('#post-dialog'),post=$('#post-content');function media(url){if(!url)return'';const m=url.match(/(?:youtu\.be\/|youtube\.com\/(?:watch\?v=|embed\/|shorts\/))([^?&/]+)/);return m?`<div class="post-media"><iframe src="https://www.youtube.com/embed/${esc(m[1])}" title="Video" allowfullscreen></iframe></div>`:`<a class="external-card" href="${esc(url)}" target="_blank" rel="noopener"><span>Open project</span><strong>${esc(url.replace(/^https?:\/\//,'').split('/')[0])} ↗</strong></a>`}function open(p){post.innerHTML=`<div class="post-kicker">${esc(section)} / ${esc(p.type||p.date)}</div><h2>${esc(p.title)}</h2><p class="post-dek">${esc(p.subtitle||p.text||'')}</p><div class="post-body">${esc(p.description||p.fullText||p.text||'').replace(/\n/g,'<br>')}</div>${media(p.embedUrl||p.link||'')}${p.details?`<div class="post-details"><div class="post-kicker">Notes / tools / stack</div>${esc(p.details).replace(/\n/g,'<br>')}</div>`:''}`;dialog.showModal()}dialog.querySelector('button').onclick=()=>dialog.close();dialog.onclick=e=>{if(e.target===dialog)dialog.close()};const items=d.projects.filter(p=>p.section===section);$('#archive-count').textContent=`${String(items.length).padStart(2,'0')} entries`;items.forEach((p,i)=>{const a=document.createElement('article');a.className=`project ${p.color||'paper'}`;a.tabIndex=0;a.innerHTML=`<div class="visual">${p.image?`<img src="${esc(p.image)}" alt="">`:`<span>${String(i+1).padStart(2,'0')}</span><i></i>`}</div><div class="project-copy"><div class="meta"><p>${esc((p.tags||[])[0]||p.type)}${p.year?' · '+esc(p.year):''}</p></div><h3>${esc(p.title)}</h3><p class="subtitle">${esc(p.subtitle)}</p></div>`;a.onclick=()=>open(p);a.onkeydown=e=>{if(e.key==='Enter')open(p)};$('#archive-grid').append(a)});if(section==='notes'){const nsec=$('#archive-notebook');nsec.hidden=false;d.notes.forEach(n=>{const a=document.createElement('article');a.tabIndex=0;a.innerHTML=`<time>${esc(n.date)}</time><div><h3>${esc(n.title)}</h3><p>${esc(n.text)}</p></div>`;a.onclick=()=>open(n);$('#archive-notes').append(a)})}
