const defaults=window.YASHMI_CONTENT;let data=defaults;try{data=JSON.parse(localStorage.getItem('yashmi-content'))||defaults}catch(e){}
const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),id=new URLSearchParams(location.search).get('id')||'research-01',p=data.projects.find(x=>x.id===id)||data.projects.find(x=>x.id==='research-01');
if(!p){document.querySelector('#reader').innerHTML='<section class="reader-hero"><h1>Not found</h1><a href="index.html">← Back home</a></section>'}else{document.title=`${p.title} - Yashmi Adani`;const tags=(p.tags||[]).map(x=>`<span>${esc(x)}</span>`).join('');document.querySelector('#reader').innerHTML=`<section class="reader-hero"><a class="reader-back" href="index.html#${esc(p.section)}">← Back to ${esc(p.section)}</a><div class="reader-number">07</div><div class="reader-meta">${esc(p.section)} / ${esc(p.type)}${p.year?' / '+esc(p.year):''}</div><h1>${esc(p.title)}</h1><p>${esc(p.subtitle)}</p></section><section class="reader-visual"><div><span>07</span><i></i></div></section><article class="reader-body"><aside><div class="reader-meta">Filed under</div>${tags}</aside><div class="reader-copy"><p class="reader-lede">${esc(p.subtitle)}</p>${esc(p.description||'').split(/\n\n+/).map(x=>`<p>${x.replace(/\n/g,'<br>')}</p>`).join('')}<div class="reader-end">End / ${esc(p.title)}</div></div></article><nav class="reader-next"><span>Continue exploring</span><a href="archive.html?section=${esc(p.section)}">View all ${esc(p.section)} →</a></nav>`}document.querySelector('#year').textContent=new Date().getFullYear();
