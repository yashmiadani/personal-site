const d=(()=>{try{return JSON.parse(localStorage.getItem('yashmi-content'))||YASHMI_CONTENT}catch(e){return YASHMI_CONTENT}})(),esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),id=new URLSearchParams(location.search).get('id'),x=(d.marginalia||[]).find(v=>v.id===id),root=document.querySelector('#essay');if(!x)root.innerHTML='<section class="reader-hero"><h1>Not found</h1></section>';else{document.title=`${x.title} - Marginalia`;const notes=x.annotations||[],paras=String(x.text||'').split(/\n\n+/).filter(Boolean);root.innerHTML=`<article class="essay-print"><a class="essay-back" href="marginalia.html">← All marginalia</a><header class="essay-title"><div class="reader-meta">${esc(x.publication)} / ${esc(x.year)}</div><h1>${esc(x.title)}</h1><p>By ${esc(x.author)}</p>${x.source?`<a href="${esc(x.source)}" target="_blank" rel="noopener">Original source ↗</a>`:''}</header><div class="essay-columns">${paras.map((p,i)=>{const ns=notes.filter(n=>Number(n.paragraph)===i+1);return `<section class="essay-row"><div class="essay-paragraph"><p>${esc(p)}</p>${ns.map((n,j)=>`<sup>${i+1}.${j+1}</sup>`).join('')}</div><aside>${ns.map((n,j)=>`<details class="margin-note"><summary>${i+1}.${j+1} / Note</summary><p>${esc(n.note)}</p>${n.passage?`<blockquote>${esc(n.passage)}</blockquote>`:''}</details>`).join('')}</aside></section>`}).join('')}</div></article>`}
